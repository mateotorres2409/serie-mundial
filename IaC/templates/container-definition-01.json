[
  {
    "name": "${name}",
    "image": "${image}",
    "essential": ${essential},
    "portMappings" : [
      {
        "containerPort" : ${containerPort},
        "hostPort"      : ${hostPort},
        "protocol": "TCP"
      }
    ]
  }
]