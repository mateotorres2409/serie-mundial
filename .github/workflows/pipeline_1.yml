name: pipeline-1
on:
    workflow_dispatch:
jobs:
    initial:   
        uses: mateotorres2409/serie-mundial-reusable@v0.0.1
        with:
            name: ${{ vars.name }}
            version_other: feature/markdown
        secrets:
            gh_pat: ${{ secrets.gh_pat }}

